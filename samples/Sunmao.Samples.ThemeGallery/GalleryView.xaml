<UserControl x:Class="Sunmao.Samples.ThemeGallery.GalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sm="urn:sunmao-wpf"
             Background="{DynamicResource BackgroundBrush}"
             TextElement.Foreground="{DynamicResource TextPrimaryBrush}"
             TextElement.FontFamily="{DynamicResource UiFontFamily}">

    <StackPanel>
        <!-- ==== Chrome bar ============================================================ -->
        <Border Style="{DynamicResource ChromeBar}">
            <DockPanel LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="Sunmao" FontSize="18" FontWeight="Bold" Margin="4,0,24,0"
                           VerticalAlignment="Center" Foreground="{DynamicResource TextOnChromeBrush}" />
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <ToggleButton Style="{DynamicResource ChromeTab}" IsChecked="True">
                        <StackPanel>
                            <sm:IconGlyph Icon="{DynamicResource IconHome}" Size="21" HorizontalAlignment="Center" />
                            <TextBlock Text="Overview" HorizontalAlignment="Center" Margin="0,3,0,0" />
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton Style="{DynamicResource ChromeTab}">
                        <StackPanel>
                            <sm:IconGlyph Icon="{DynamicResource IconScan}" Size="21" HorizontalAlignment="Center" />
                            <TextBlock Text="Devices" HorizontalAlignment="Center" Margin="0,3,0,0" />
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton Style="{DynamicResource ChromeTab}">
                        <StackPanel>
                            <sm:IconGlyph Icon="{DynamicResource IconSettings}" Size="21" HorizontalAlignment="Center" />
                            <TextBlock Text="Settings" HorizontalAlignment="Center" Margin="0,3,0,0" />
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <sm:ConnectionPill Text="3 devices online" IsOnline="True" Margin="0,0,16,0" />
                    <Button Style="{DynamicResource ChromeIconButton}" AutomationProperties.Name="Help">
                        <sm:IconGlyph Icon="{DynamicResource IconHelp}" />
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <StackPanel Margin="{DynamicResource PageGutter}">
            <sm:PageHeader Title="Theme gallery" Subtitle="Every Sunmao.Wpf.Theme style and control">
                <sm:PageHeader.Actions>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Export" Margin="0,0,8,0" />
                        <Button Style="{DynamicResource PrimaryButton}">
                            <StackPanel Orientation="Horizontal">
                                <sm:IconGlyph Icon="{DynamicResource IconPlus}" Size="16" Margin="0,0,6,0" />
                                <TextBlock Text="New job" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </sm:PageHeader.Actions>
                <sm:PageHeader.Secondary>
                    <Border Style="{DynamicResource SegmentedHost}">
                        <StackPanel Orientation="Horizontal">
                            <ToggleButton Style="{DynamicResource SegmentTab}" Content="Controls" IsChecked="True" />
                            <ToggleButton Style="{DynamicResource SegmentTab}" Content="Status" />
                            <ToggleButton Style="{DynamicResource SegmentTab}" Content="Icons" />
                        </StackPanel>
                    </Border>
                </sm:PageHeader.Secondary>
            </sm:PageHeader>

            <!-- ==== Metrics ========================================================== -->
            <UniformGrid Columns="4" Margin="0,0,0,16">
                <sm:MetricTile Label="Jobs today" Value="1,284" Caption="since 00:00" Margin="0,0,10,0" />
                <sm:MetricTile Label="Completed" Value="1,201" Caption="93.5 %" Accent="Success" Margin="0,0,10,0" />
                <sm:MetricTile Label="Queued" Value="71" Caption="waiting" Accent="Warning" UseAccentBackground="True" Margin="0,0,10,0" />
                <sm:MetricTile Label="Failed" Value="12" Caption="0.9 %" Accent="Danger" UseAccentBackground="True" />
            </UniformGrid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- ==== Left column =================================================== -->
                <StackPanel>
                    <sm:InfoCard Header="Buttons">
                        <sm:InfoCard.Body>
                            <StackPanel>
                                <WrapPanel>
                                    <Button Style="{DynamicResource PrimaryButton}" Content="Primary" Margin="0,0,8,8" />
                                    <Button Content="Secondary" Margin="0,0,8,8" />
                                    <Button Style="{DynamicResource GhostButton}" Content="Ghost" Margin="0,0,8,8" />
                                    <Button Style="{DynamicResource DangerButton}" Content="Delete" Margin="0,0,8,8" />
                                    <Button Style="{DynamicResource IconButton}" AutomationProperties.Name="Search" Margin="0,0,8,8">
                                        <sm:IconGlyph Icon="{DynamicResource IconSearch}" />
                                    </Button>
                                    <Button Style="{DynamicResource PrimaryButton}" Content="Disabled" IsEnabled="False" Margin="0,0,8,8" />
                                </WrapPanel>
                                <Button Style="{DynamicResource LinkButton}" Content="Link button" HorizontalAlignment="Left" />
                            </StackPanel>
                        </sm:InfoCard.Body>
                    </sm:InfoCard>

                    <sm:InfoCard Header="Inputs" Margin="0,16,0,0">
                        <sm:InfoCard.Body>
                            <StackPanel>
                                <TextBlock Text="Job name" Style="{DynamicResource CaptionText}" Margin="0,0,0,4" />
                                <TextBox Text="nightly-export" Margin="0,0,0,12" />
                                <TextBlock Text="Target device" Style="{DynamicResource CaptionText}" Margin="0,0,0,4" />
                                <ComboBox SelectedIndex="0" Margin="0,0,0,12">
                                    <ComboBoxItem Content="Pump controller A" />
                                    <ComboBoxItem Content="Pump controller B" />
                                </ComboBox>
                                <TextBlock Text="Sample rate" Style="{DynamicResource CaptionText}" Margin="0,0,0,4" />
                                <Slider Minimum="0" Maximum="1" Value="0.45" Margin="0,0,0,12" />
                                <WrapPanel>
                                    <CheckBox Content="Keep history" IsChecked="True" Margin="0,0,18,8" />
                                    <CheckBox Content="Partial" IsChecked="{x:Null}" IsThreeState="True" Margin="0,0,18,8" />
                                    <RadioButton Content="Fast" IsChecked="True" GroupName="mode" Margin="0,0,18,8" />
                                    <RadioButton Content="Precise" GroupName="mode" Margin="0,0,18,8" />
                                </WrapPanel>
                                <CheckBox Style="{DynamicResource ToggleSwitch}" Content="Auto start" IsChecked="True" Margin="0,4,0,0" />
                                <ProgressBar Value="62" Margin="0,16,0,0" />
                            </StackPanel>
                        </sm:InfoCard.Body>
                    </sm:InfoCard>

                    <sm:InfoCard Header="Health checks" Margin="0,16,0,0">
                        <sm:InfoCard.Body>
                            <StackPanel>
                                <sm:CheckRow Icon="{DynamicResource IconDisk}" Title="Disk space" Detail="128 GB free" State="Success" />
                                <sm:CheckRow Icon="{DynamicResource IconPlug}" Title="Controller link" Detail="Retrying every 5 s" State="Warning" />
                                <sm:CheckRow Icon="{DynamicResource IconCube}" Title="Configuration" Detail="No profile selected" State="Danger" />
                                <sm:CheckRow Icon="{DynamicResource IconClock}" Title="Schedule" Detail="Not checked yet" />
                            </StackPanel>
                        </sm:InfoCard.Body>
                    </sm:InfoCard>
                </StackPanel>

                <!-- ==== Right column ================================================== -->
                <StackPanel Grid.Column="2">
                    <sm:InfoCard Header="Status">
                        <sm:InfoCard.Body>
                            <StackPanel>
                                <WrapPanel Margin="0,0,0,12">
                                    <sm:StatusChip Text="Neutral" Margin="0,0,8,8" />
                                    <sm:StatusChip Text="Complete" Kind="Success" ShowDot="True" Margin="0,0,8,8" />
                                    <sm:StatusChip Text="Partial" Kind="Warning" ShowDot="True" Margin="0,0,8,8" />
                                    <sm:StatusChip Text="Failed" Kind="Danger" ShowDot="True" Margin="0,0,8,8" />
                                    <sm:StatusChip Text="Running" Kind="Info" Icon="{DynamicResource IconRefresh}" Margin="0,0,8,8" />
                                </WrapPanel>
                                <sm:NoticeBar Kind="Info" Text="Exports never modify the source data." Margin="0,0,0,8" />
                                <sm:NoticeBar Kind="Success" Text="All 3 devices are connected." Margin="0,0,0,8" />
                                <sm:NoticeBar Kind="Warning" Text="Disk usage is above 85 %; the oldest logs will be pruned first." Margin="0,0,0,8" />
                                <sm:NoticeBar Kind="Danger" Text="Logging stopped: the data folder could not be written. Check the drive." />
                            </StackPanel>
                        </sm:InfoCard.Body>
                    </sm:InfoCard>

                    <sm:InfoCard Header="Recent jobs" Margin="0,16,0,0">
                        <sm:InfoCard.HeaderAction>
                            <Button Style="{DynamicResource LinkButton}" Content="See all" />
                        </sm:InfoCard.HeaderAction>
                        <sm:InfoCard.Body>
                            <StackPanel>
                                <sm:ListRow Title="Nightly export" Meta="09:42 · device A" Detail="240 records" ChipText="Complete" ChipKind="Success" />
                                <sm:ListRow Title="Calibration sweep" Meta="10:15 · device B" Detail="118 records" ChipText="Partial" ChipKind="Warning" />
                                <sm:ListRow Title="Weekly report" Meta="16:03 · yesterday" ChipText="Archived" ShowChevron="False" />
                            </StackPanel>
                        </sm:InfoCard.Body>
                    </sm:InfoCard>

                    <sm:InfoCard Header="Data" Margin="0,16,0,0">
                        <sm:InfoCard.Body>
                            <StackPanel>
                                <TabControl>
                                    <TabItem Header="Jobs" IsSelected="True" />
                                    <TabItem Header="Events" />
                                    <TabItem Header="Log" />
                                </TabControl>
                                <DataGrid x:Name="SampleGrid" AutoGenerateColumns="True" IsReadOnly="True" Height="170" Margin="0,4,0,0" />
                            </StackPanel>
                        </sm:InfoCard.Body>
                    </sm:InfoCard>
                </StackPanel>
            </Grid>

            <!-- ==== Empty and drop states =============================================== -->
            <Grid Margin="0,16,0,0" Height="330">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border Style="{DynamicResource Card}">
                    <sm:EmptyState Icon="{DynamicResource IconLayers}"
                                   Title="No projects yet"
                                   Description="Create or import a project to get started.">
                        <sm:EmptyState.Action>
                            <Button Style="{DynamicResource PrimaryButton}" Content="Import project" />
                        </sm:EmptyState.Action>
                    </sm:EmptyState>
                </Border>
                <sm:DropZone Grid.Column="2"
                             Icon="{DynamicResource IconFolderUp}"
                             Title="Drop files here"
                             Description="Files are copied into a new project."
                             Hint="CSV, JSON or XML"
                             Note="Importing never modifies the source files.">
                    <sm:DropZone.Action>
                        <Button Style="{DynamicResource PrimaryButton}" Content="Choose files" />
                    </sm:DropZone.Action>
                </sm:DropZone>
            </Grid>

            <!-- ==== Icons ============================================================== -->
            <sm:InfoCard Header="Icons" Margin="0,16,0,0">
                <sm:InfoCard.Body>
                    <ItemsControl x:Name="IconList">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="112" Margin="0,0,0,14">
                                    <sm:IconGlyph Icon="{Binding Geometry}" Size="22" HorizontalAlignment="Center"
                                                  Foreground="{DynamicResource TextPrimaryBrush}" />
                                    <TextBlock Text="{Binding Name}" Style="{DynamicResource MetaText}" HorizontalAlignment="Center" Margin="0,6,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </sm:InfoCard.Body>
            </sm:InfoCard>

            <!-- ==== Typography ========================================================= -->
            <sm:InfoCard Header="Typography" Margin="0,16,0,0">
                <sm:InfoCard.Body>
                    <StackPanel>
                        <TextBlock Text="Page title" Style="{DynamicResource PageTitleText}" />
                        <TextBlock Text="Section title" Style="{DynamicResource SectionTitleText}" Margin="0,8,0,0" />
                        <TextBlock Text="Row title" Style="{DynamicResource RowTitleText}" Margin="0,8,0,0" />
                        <TextBlock Text="Body text wraps across the card when it is long, so paragraphs stay readable at any width." Style="{DynamicResource BodyText}" Margin="0,8,0,0" />
                        <TextBlock Text="Caption text" Style="{DynamicResource CaptionText}" Margin="0,8,0,0" />
                        <TextBlock Text="Meta text · 2026-09-18 10:42" Style="{DynamicResource MetaText}" Margin="0,8,0,0" />
                    </StackPanel>
                </sm:InfoCard.Body>
            </sm:InfoCard>
        </StackPanel>
    </StackPanel>
</UserControl>
